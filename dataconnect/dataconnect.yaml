specVersion: "v1beta"
serviceId: "voteSystem"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "voteSystem-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
